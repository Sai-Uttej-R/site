 ```
# Site

This directory contains the files for saiuttej r's personal site.

## Files

* `index.html`: The main HTML file for the site.
* `styles.css`: The CSS file for the site.
* `script.js`: The JavaScript file for the site.
* `images/`: The directory containing the images used on the site.

## Setup

To set up the site, you will need to:

1. Clone the repository to your local machine.
2. Install the dependencies.
3. Start the development server.

### Cloning the Repository

To clone the repository, run the following command in your terminal:

```
git clone https://github.com/user/site.git
```